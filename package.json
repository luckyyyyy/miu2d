{
  "name": "miu2d",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @miu2d/web dev",
    "build": "pnpm -r build",
    "build:wasm": "bash scripts/build-wasm.sh",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check --write .",
    "tsc": "pnpm -r tsc",
    "preview": "pnpm --filter @miu2d/web preview"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.13",
    "typescript": "~5.9.3"
  },
  "overrides": {
    "vite": "npm:rolldown-vite@7.2.5"
  }
}
